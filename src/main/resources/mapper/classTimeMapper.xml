<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.BaseProject.dao.ClassTimeMapper">
    <select id="selectAll" resultType="ClassTimeDto">
        select id, class_start_time, class_end_time
        from class_time
    </select>
</mapper>